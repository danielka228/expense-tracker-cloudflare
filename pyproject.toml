[project]
name = "expense-tracker-cloudflare"
version = "1.0.0"
description = "Expense Tracker - Flask app"
requires-python = ">=3.12"
dependencies = [
    "flask",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]

[tool.setuptools]
py-modules = ["app", "templates"]